/**
 * ⛔ ScrollCTAHandler - DISABLED (Google Ads Compliance)
 * 
 * Scroll-triggered popups + auto WhatsApp redirect violates Google Ads:
 * - "Compromised site / Abusing the ad network"
 * - "Misleading behavior: opening new windows without user interaction"
 * 
 * Use SafeLeadInline, SafeModalOnClick, or SafeStickyBar instead
 * WhatsApp should only open on explicit user click
 */
export default function ScrollCTAHandler() {
  return null;
}